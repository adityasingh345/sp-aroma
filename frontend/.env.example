# Backend API Configuration
# =========================

# Backend API URL (optional)
# Leave empty to use relative paths with Vite proxy (recommended for development)
# Set to your backend URL for production deployment
VITE_API_BASE=

# Example configurations:
# Development with backend on different port:
# VITE_API_BASE=http://localhost:8000

# Production with hosted backend:
# VITE_API_BASE=https://api.yourdomain.com

# Development with Vite proxy (recommended):
# VITE_API_BASE= (leave empty)
