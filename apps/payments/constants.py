class PaymentStatus:
    CREATED = "created"
    SUCCEEDED = "succeeded"
    FAILED = "failed"

class OrderStatus:
    PENDING = "pending"
    PAID = "paid"
    FAILED = "failed"
    CANCELLED = "cancelled"
